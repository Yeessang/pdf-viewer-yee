{
  "name": "@pdf-viewer-yee/core",
  "version": "1.0.2",
  "description": "",
  "main": "dist/bundle.js",
  "private": false,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.js --bundle --outfile=dist/bundle.js --format=esm --global-name=Core --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "pdfjs-dist": "^3.11.174"
  },
  "devDependencies": {
    "esbuild": "^0.25.1"
  }
}
